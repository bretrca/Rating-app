import {Card} from '../../models/element';
import {addStar,reduceStar} from '../film.actions';
import *  as types  from '../action.types';
import {initialState} from '../film.state';


export function reduceStarReducer (state = initialState, action:reduceStar){
    /* switch(action.type){
        case REDDUCE_STAR:
            return REDUCE_STAR;
    } */
} 